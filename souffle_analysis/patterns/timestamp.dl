.decl timestampEquality()
timestampEquality():-
    builtin(blocknumberUnit, _, "BLOCKNUMBER"),
    builtin(equalityFunUnit, _, "eq"),
    appDD(appDDUnit, _),
    argument(blocknumberUnit, appDDUnit, _),
    dependsOn(appDDUnit, equalityFunUnit).

.output timestampEquality