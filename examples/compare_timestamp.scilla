scilla_version 0

(* MatchStatement contract *)

contract MatchStatement
()

field msg : Bool = True

transition test (a: BNum)
  t <- & BLOCKNUMBER;
  e = builtin eq a t;
  match e with
  | False =>
    msg := e
  | True =>
    msg := e
  end
end
